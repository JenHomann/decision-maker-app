<%= form_for(@round, method: :post, url: create_contacts_path(@round.encrypted_url)) do |f| %>
  <% if @round.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@round.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @round.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.fields_for :contacts do |c| %>
    <div class="field">
    <%= c.label :name %><br>
    <%= c.text_field :name %>
    </div>
    <div class="field">
      <%= c.label :email %><br>
      <%= c.text_field :email%>
    </div>
  <% end %>
  
  <div class="actions">
  <%= f.submit "Submit", class: "button radius" %>
  </div>
<% end %>